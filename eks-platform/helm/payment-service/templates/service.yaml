apiVersion: v1
kind: Service

metadata:
  name: payment-service
  namespace: {{ .Values.namespace }}

spec:
  selector:
    app: payment

  ports:
  - port: {{ .Values.service.port }}
    targetPort: 80

  type: {{ .Values.service.type }}
